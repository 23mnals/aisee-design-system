# Web 产物同步与分支清理

## 本轮目标

- 同步 Web ChatGPT 产出的 Managed Automation 页面。
- 核对 Brand 分类、Legacy 展示、开发分支与临时 worktree 状态。

## 本轮完成

- Brand 侧边栏按 AIsee 功能模块整理为 Homepage、Common、Overview、Analyze、Growth、Post、Engage、Verify、Automation。
- Legacy 页面改为标题旁轻量灰色小写 `legacy`；移除侧边栏 Legacy 标签和标题栏右侧重复的大写标签。
- Brand 分类与 Legacy 批次通过 `npm run check`，39 项测试通过，并通过 PR #3 合并到 `main`。
- 拉取并确认 Managed Automation 产物：`preview.html`、`preview.png`、`meta.json`，同时在 `brand/pages/index.json` 完成注册。
- 当时本地预览 `http://127.0.0.1:4173/` 的首页、index 注册和 Managed Automation HTML 均返回 200。
- 删除本地与远端临时分支 `ai/web/brand-source-status` 及对应 worktree；保留 `ai/desktop/design-system-checkpoint`。

## 修改文件

- `aisee-design-system-preview.html`
- `brand/README.md`
- `tests/preview.test.mjs`
- `brand/pages/index.json`
- `brand/pages/managed-automation/preview.html`
- `brand/pages/managed-automation/preview.png`
- `brand/pages/managed-automation/meta.json`
- `handoff-context.md`

## Git / Commit / PR

- PR #3 merge commit：`3bdccf4`。
- Managed Automation commit：`c6ace77 feat(brand): add Managed Automation preview`。
- 本轮结束时 `origin/ai/desktop/design-system-current` 为 `c6ace77`，`origin/main` 为 `3bdccf4`，开发分支领先 1 个提交。
- 本地 `main` 仍停在 `2358f97`，不能用它代替 `origin/main` 判断正式状态。

## 本轮确认的设计决策

- Automation 是独立 WORKFLOWS 功能，不归入 Connections / Search Console。
- Web 产物元数据为 `source: ChatGPT`、`surface: Web`；Managed Automation 当前为 `designStatus: Draft`。
- Web 产物状态与必需文件规则现已归入 `AGENTS.md` 和 `docs/TEAM_DECISIONS.md`。

## 遇到的问题及处理

- 临时 Web 分支和 worktree 会造成同一 Design System 状态分散；本轮已清理已完成的临时分支，保留长期开发分支与 checkpoint。
- `main` 的本地指针落后于远端，因此所有正式状态判断均改为同时核对 `origin/main` 与实时 Git 状态。

## 未完成

- `c6ace77` 尚未进入 `main`。
- 合并/同步后仍需检查 Actions / Pages，并实际打开线上 `Brand → Automation → Managed Automation` 验证资源与交互。

## 下一轮建议

- 先保护本地未提交修改，再按用户确认的流程处理开发分支与 `main` 同步。
- 不恢复已删除的旧 Web worktree，不删除 checkpoint，除非用户另行明确要求。
