# Main 发布规则与正式同步 PR

## 本轮目标

- 调整 `main` 发布授权规则。
- 将 `ai/desktop/design-system-current` 的当前内容提交为进入 `main` 的 PR，但不自动合并。

## 本轮完成

- 更新 `AGENTS.md`：`main` 不直接 push；用户明确要求发布或同步到 `main` 时，授权创建 `design-system-current → main` PR。
- 明确 PR 不自动 merge；只有用户同时明确要求合并时才执行。
- 创建 PR #4：`https://github.com/23mnals/aisee-design-system/pull/4`。
- PR 当前保持开放，未修改 `main`，未执行 merge。

## Git / PR

- 开发分支：`ai/desktop/design-system-current`。
- Base：`main`。
- 发布规则提交：`ee29cab docs: define main publication authorization`。
- PR #4 包含 Managed Automation、最新 keep-open eye demo、交接机制重构与发布授权规则。

## 验证

- 创建 PR 前确认不存在相同 head/base 的开放 PR。
- 已确认开发分支包含 `f6e434192e5ca4330f2d6ae3ad416c61337f6a90`。
- 前一轮 `npm run check` 已通过 39 项测试、typecheck、token 检查与 build。

## 未完成 / 下一步

- 等待 PR #4 审阅。
- 只有用户明确要求合并时才 merge；合并后检查 Actions / Pages 与线上 Managed Automation 页面。
