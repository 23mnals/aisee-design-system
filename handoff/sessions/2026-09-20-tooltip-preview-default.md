# Tooltip 预览默认 Playful · 2026-09-20

用户认可新版弹簧并要求默认显示 playful。Tooltip / Toast Demo 的动画初始选择已改为 playful，Subtle 选项去掉易混淆的 default 标注。通用 Tooltip API 默认 subtle 保留；Copy for AI 读取初始 playful 与切换后的实际选择。README、配置规范、AI_HANDOFF 与当前交接同步。

沿用 ai/desktop/design-system-current，HEAD d6d166b；本轮无 commit / push，已有工作区修改保留。重建演示与静态站点，重新加载页面检查初始选项和配置快照。
